import { motion } from "framer-motion";
import { Input } from "./ui/input";

// Enhance the Input component with motion capabilities using framer-motion
// This allows the Input component to accept animation props and animate accordingly
const MotionInput = motion(Input);

export default MotionInput;
